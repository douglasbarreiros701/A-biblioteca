<!DOCTYPE html>
<html lang="en">
    <link rel="stylesheet" href="/styleEdit.css">
    <link rel="icon" href="/imgs/aba-livro.png">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>editt</title>
</head>
<body>
    <div class="container">
            <div class="containerInput">
                <form action="/edit/<%=body.id%>" method="POST">
                    <input  name="title" placeholder="Titulo" required value="<%=body.title%>"/>
                    <input  name="author" placeholder="Autor" required value="<%=body.author%>"/>
                    <input id="placeholderData"  name="date" placeholder="dd/mm/aaaa" required value="<%=body.date%>"/>
                    <input id="botao" type="submit" value="EDITAR">
                </form>
            </div>
        </div>


        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.mask/1.14.10/jquery.mask.js"></script>

        <!-- Mascara do imput "campoData" -->
        <script type="text/javascript">
            $(document).ready(function(){
                $('#placeholderData').mask('99/99/9999')
            })
            
            // let input = document.getElementById("data")

            // input.addEventListener("keypress", ()=>{
            //     let inputLength = input.value.length
                
            //     if(inputLength === 2 || inputLength === 5){
            //         input.value += "/" 
            //     }
            // })
        </script>


</body>
</html>